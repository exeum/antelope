sudo: required
language: python
python:
  - "3.6"
services:
  - docker
#install:
#  - pip install -r scraper/requirements.txt pylint
script:
  - find . -name '*.py' | xargs pylint || true
  #- find . -name '*.py' | xargs pylint && STATUS=0 || STATUS=$?
  #- test $(( STATUS & 3 )) -ne 0 && exit $STATUS
  - ( cd scraper && docker build -t antelope-scraper . )
  - ( cd deliverer && docker build -t antelope-deliverer . )
notifications:
  slack: exeum-foundation:N5NC7XlSG6BlY85rF3aILVG6
